# 001 — Core ledger (model, storage, add/list)

## Summary

Foundational expense model, local storage, and `add` / `list` commands.

## Prerequisites

None

## Acceptance

- Amounts stored as integer cents.
- `add` and `list` commands with tests.
